const stringUpperCase = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const joinStrings = ((a, b) => a + b);

module.exports = {stringUpperCase, firstLetter, joinStrings};

